# 리딩쉴드(ReadingShield) — 통화 녹음 기반 사후 보이스피싱 탐지 기능

이 문서는 Claude Code가 이 기능을 처음부터 구현할 수 있도록 배경, 설계 결정, 작업 순서를 정리한 브리핑입니다.

---

## 1. 프로젝트 배경

- **프로젝트명**: 리딩쉴드 (ReadingShield)
- **맥락**: 2026 Google-Ajou University AI Convergence Capstone Design 경진대회 출품작, 10주 개발 일정
- **핵심 목적**: 투자 리딩방(리딩방 사기)·보이스피싱 등 투자 스캠을 스크린샷/채팅/통화 내용 분석을 통해 탐지하고, 금융감독원(FSS) API와 대조해 사용자에게 대응 방법을 안내
- **기존에 구축된 부분** (이미 설계 완료, 재사용 대상):
  - 2단계 하이브리드 분류 파이프라인: 경량 키워드 필터 → Gemini 기반 컨텍스트 분류
  - Vertex AI로 모델 서빙
  - Gemini 멀티모달로 스크린샷 OCR
  - FSS API 크로스레퍼런스로 사기 패턴/블랙리스트 대조
  - 전달 채널: 카카오톡 채널 챗봇 또는 모바일 웹앱

## 2. 이번에 추가하는 기능: 통화 녹음 사후 분석

### 2.1 목표
사용자(갤럭시 기기)가 삼성 기본 전화 앱의 "통화 자동 녹음" 기능을 켜둔 경우, 그 녹음 파일을 앱이 자동으로 찾아 분석하고, 보이스피싱 의심 패턴이 발견되면 사용자에게 알림.

### 2.2 스코프 밖 (의도적으로 제외)
- **실시간 통화 감청/개입**: Android 정책상 서드파티 앱은 통화 중 오디오 스트림에 접근 불가 (Android 10+ API 제한, 2022년 이후 접근성 API를 통한 우회도 차단됨). 이 기능은 시도하지 않음.
- **발신번호 자동 매칭**: `READ_CALL_LOG` 권한은 구글이 "기본 전화/SMS/어시스턴트 앱"에게만 허용하는 제한 권한이라, 특화 앱인 우리 앱은 신청 자격이 없음. 번호 매칭은 하지 않고, 통화 **내용 텍스트 패턴**만으로 탐지.
- **24시간 상시 이벤트 리스닝(포그라운드 서비스 + TelephonyCallback)**: 기술적으로는 가능하나, 상시 알림 노출·Play 심사 부담(`specialUse` 포그라운드 서비스 타입 소명)·배터리 이슈·OS 강제종료 리스크가 커서 채택하지 않음. 아래 2.3 참고.

### 2.3 채택한 설계: 주기 폴링 (WorkManager)
이 기능은 실시간 개입이 아니라 **사후 탐지**이므로, 통화 종료 직후 몇 초 안에 반응할 필요가 없음. 대신:

- `PeriodicWorkRequest`로 **15분 간격** (WorkManager 최소 주기) 폴링
- 매 실행마다 `MediaStore.Audio`를 쿼리해서 마지막 체크 이후 새로 추가된 `Recordings/Call` 폴더 파일 확인
- 새 파일 발견 시 분석 파이프라인(STT → 텍스트 분류)에 전달
- 포그라운드 서비스, 상시 알림, `TelephonyCallback`, `READ_PHONE_STATE` 모두 불필요

**이 방식을 택한 이유**: 감지 지연이 최대 15분 생기지만, 사용자에게 "이 통화는 사기였을 가능성이 있다, 신고/차단하라"는 사후 안내가 목적이므로 15분 지연이 기능 가치를 거의 훼손하지 않음. 대신 구현 복잡도, Play 심사 리스크, 배터리 부담을 크게 줄일 수 있음.

### 2.4 분석 파이프라인 재사용
통화 녹음은 새 모델을 만들 필요 없이, **기존 스크린샷 분석용 2단계 파이프라인(키워드 필터 → Gemini 분류)에 입력 소스만 추가**하는 개념으로 접근:

```
녹음 파일(.m4a/.mp3) → STT 변환(Google Cloud Speech-to-Text) → 텍스트
    → [기존] 키워드 필터 → Gemini 컨텍스트 분류 → FSS API 대조
    → 결과: 사기 의심 여부 + 근거
```

단, 통화 텍스트는 STT 특성상 구어체·비문법적 표현·끊김이 많으므로, 프롬프트/전처리 단계에서 스크린샷 텍스트와는 다른 튜닝이 필요할 수 있음.

---

## 3. 작업 순서 (Phase)

### Phase 0 — 실기기 검증 스파이크 (최우선, 착수 전 필수)
목적: 아래 가정들이 실제 갤럭시 기기에서 유효한지 확인. 여기서 막히면 이후 Phase 진행 무의미하므로 반드시 먼저 수행.

확인할 것:
1. 삼성 기본 전화 앱에서 "통화 자동 녹음" 켠 상태로 실제 통화 → 녹음 파일 생성 확인
2. `MediaStore.Audio.Media.EXTERNAL_CONTENT_URI` 쿼리로 이 파일이 인덱싱되어 조회되는지
3. `RELATIVE_PATH` 실제 값 확인 (문서상 `Recordings/Call/`로 추정되나 One UI 버전별 상이 가능)
4. 통화 종료 시점부터 파일이 인덱싱되기까지 걸리는 시간(대략치)
5. 테스트 기기의 One UI / Android 버전 기록

산출물: 위 확인 결과를 정리한 짧은 리포트 (성공/실패 여부, 실제 경로값, 특이사항)

→ **실패 시**: 통화 녹음 기능 전체를 스코프에서 제외하고 스크린샷 분석에 집중하는 것으로 전환. 이 경우 Phase 1, 2는 진행하지 않음.

### Phase 1 — 감지 파이프라인 구현
Phase 0 성공을 전제로 진행.

1. `PeriodicWorkRequest` 등록 (15분 간격, 네트워크 연결 제약 조건)
2. 마지막 체크 시각 저장/조회 로직 (SharedPreferences 또는 DataStore)
3. `MediaStore.Audio` 쿼리로 신규 녹음 파일 탐색
4. 신규 파일 발견 시 `OneTimeWorkRequest`로 분석 작업(Phase 2) 큐잉
5. 필요 권한: `READ_MEDIA_AUDIO`(Android 13+), `READ_EXTERNAL_STORAGE`(maxSdkVersion=32, 하위 호환)

### Phase 2 — 분석 및 알림
1. 녹음 파일 오디오 바이트 읽기
2. Google Cloud Speech-to-Text 연동 (기존 Vertex AI 스택과 같은 GCP 프로젝트 사용)
3. 변환된 텍스트를 기존 스크린샷 분석 파이프라인(키워드 필터 → Gemini 분류)에 전달
4. FSS API 대조
5. 사기 의심 시 로컬 알림(Notification) 발송, 신고/대응 가이드 표시

### Phase 3 — 온보딩/권한 UX
1. 권한 요청 사전 설명 화면 (`READ_MEDIA_AUDIO`가 필요한 이유 명시)
2. 기능 온/오프 토글 (기본값 OFF, 원하는 사용자만 활성화)
3. 개인정보처리방침에 "통화 녹음 파일 로컬 분석 + 서버 전송 여부" 명확히 기재

---

## 4. 제약사항 및 참고 정보

- **타겟 범위**: 삼성 갤럭시 기기 우선 고려 (국내 안드로이드 점유율 기준). 자동 녹음 기능이 없거나 꺼져 있는 사용자에게는 이 기능 자체가 작동하지 않음 — 전체 사용자 대비 부분 커버리지라는 한계를 명확히 인지.
- **WorkManager 주기 작업 최소 간격은 15분** (더 짧게 설정 불가, 시스템이 무시함)
- **`READ_CALL_LOG`는 절대 사용하지 않음** — 기본 전화 앱 자격이 없으면 Play 심사 통과 불가
- **한국은 일방 동의 녹음이 합법** (본인이 참여한 통화는 상대 동의 없이 녹음 가능) — 이 전제 하에 기능이 성립. 법적 이슈 없음.
- STT 및 텍스트 분류 API 호출은 네트워크 필요 (WorkManager 제약 조건에 `NetworkType.CONNECTED` 설정 필요)

## 5. Claude Code에게 요청하는 산출물

1. Phase 0 검증용 최소 테스트 앱/스크립트 (MediaStore 쿼리 + 로그 출력)
2. Phase 0 결과에 따라 Phase 1~3 진행 여부 판단 후 보고
3. (Phase 0 성공 시) Phase 1~2 Android 모듈 구현 — Kotlin, 기존 프로젝트 구조에 통합
4. 각 단계별 필요 권한/매니페스트 변경사항 diff로 제시
5. 테스트 방법 (실기기 필요한 부분은 명시적으로 표시)
